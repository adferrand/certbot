[testenv]
deps = pytest
       pytest-timeout
       pytest-xdist
       pyreadline
commands = pip install -e acme
           pytest --numprocess auto --timeout 30 --pyargs acme
           pytest --numprocess auto --timeout 30 --pyargs certbot
